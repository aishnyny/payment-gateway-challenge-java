server.port=8090
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# Base URL of the acquiring bank simulator. Configurable rather than
# hardcoded so it can be pointed at a different address per environment.
bank.simulator.base-url=http://localhost:8080